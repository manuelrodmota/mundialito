# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude-temp/
.codex-temp/
.claude-backups/
.codex-backups/
qubika-agentic-framework/
.mcp.json

# Supabase local env (contains service-role key — never commit)
.env.local
supabase/.temp/
supabase/out/
.vercel

# Raw source CSVs — byte-identical duplicate of supabase/seed/ (the canonical copy
# the seed script reads). Kept locally as an archive; not tracked.
datasets/
