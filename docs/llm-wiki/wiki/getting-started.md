# Getting started — client

_Generated by the AI Agentic Framework. Source-of-truth for "how to run this project" — derived from your `Makefile` / `Justfile` / `Taskfile` / setup scripts (Tier 1), README "Getting Started" sections (Tier 2), per-service package-manager scripts (Tier 3), and CI hints (Tier 4)._

## From the project README

_Reproduced verbatim from the README — the original wording is authoritative for any quirks the wrapper / package-manager tables below may miss._

### How to Run It Locally

_Source: `README.md` § How to Run It Locally_

> The app reads its game data (players, teams, ratings) from a **local Supabase Postgres** —
> without it the client throws on boot and the UI renders empty. First-time setup is the full
> chain below, not just `install` + `dev`.
>
> **No Supabase account needed.** Everything runs locally — `supabase start` spins up the whole
> stack on your machine, and the player/team data ships as committed CSVs in `supabase/seed/`.
> The keys you'll see are Supabase's universal local demo keys (the same on every machine).

## Per-service commands (low-level)

> **Prefer the wrapper above when present; these run a single service in isolation and may not start dependent services (databases, queues, identity providers).** Use them when you need to iterate on a single service inside an already-running wrapper-managed environment.

| Action | Service | Command | Source |
|---|---|---|---|
| Start dev server | `_root` | `vite` | `documented_commands(package_json)` |
| Run tests | `_root` | `vitest run` | `documented_commands(package_json)` |
| Build | `_root` | `tsc -b && vite build` | `documented_commands(package_json)` |
| Lint | `_root` | `eslint .` | `documented_commands(package_json)` |
